# welcome to the 21th century
alias cat='bat'
alias diff='delta'
alias df='duf'
alias du='dust'
alias find='fd'
alias ls='exa'
alias vi='nvim'
alias vim='nvim'

# dangerous
alias cut='choose'
alias grep='rg'
alias ps='procs'

# safety first
alias mkdir='mkdir -pv'
alias mv='mv -iv'
alias rm='rm -v'
alias cp='cp -iv'

# goes to clipboard selection
alias yank='yank -- xsel -b'

# ls options
alias ls='ls -h --color=always --group-directories-first'
alias la='ls -al'  # all files and dirs
alias lh='ls -a | egrep "^\."' # only hidden
alias ll='ls -l'   # long format
alias lt='ls -aT | bat'  # tree listing

alias l='ls'

# correct typos
alias pytohn='python'
alias vu='nvim'
alias vo='nvim'

# pipenv stuff
alias py='pipenv run python'
alias ipy='pipenv run ptpython'
alias ppy='pipenv run ptpython'
alias flask='pipenv run python -m flask'

# git stuff
alias gc='git commit'
alias gd='git diff'
alias gl='git log --graph --decorate --oneline --abbrev-commit'
alias gs='git status'

# vim stuff
alias vsp='nvim -O'
alias vdf='nvim -d'
alias vtb='nvim -p'
alias fastvim='nvim -n -u NONE -i NONE -N'

# misc
alias anki="~/Downloads/anki-2.1.35-linux-amd64/bin/anki"
